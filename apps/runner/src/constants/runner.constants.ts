/**
 * Must match `action` stored on ApprovalExecutionToken when human approves a shell run.
 * Create approval flows for risky commands with action = RUNNER_EXEC_ACTION.
 */
export const RUNNER_EXEC_ACTION = 'runner.exec';
