/**
 * Jest global setup
 */

jest.setTimeout(30000);

beforeAll(async () => {
  // Global setup hook placeholder
});

afterAll(async () => {
  // Global cleanup hook placeholder
});

beforeEach(() => {
  jest.clearAllMocks();
});
