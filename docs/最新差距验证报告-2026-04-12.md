# 最新差距验证报告

> **验证日期**：2026-04-12  
> **合并后修订**（P7 + P1，CPO 已通过）：**G1 / G2 / G3** 状态更新为 **「已部分修复」**，详见 §2；§1 中「初版静态快照」条目保留作历史对照。  
> **基准文档**：`docs/当前对齐差距报告.md`、`docs/多租户隔离-实施交付物-PR清单-PRD架构K8s与QA.md`、根目录 `当前实现对齐检查清单.md`  
> **方法**：对仓库重新执行与原差距报告一致的 **Glob / Grep / Read**；未做生产环境运行时验证。

---

## 0. 本报告范围说明（与此前长回复的关系）

| 内容 | 是否在本文件中 |
|------|----------------|
| **复跑核查命令 + G1～G8 状态 + 与清单 A～D 对照** | **是**（本文件主体） |
| 《当前对齐差距报告》中的静态结论复核（数据层/执行层/架构/可观测） | **是**（§3、§4） |
| **P1～P8 逐条修复方案**、**阶段 1～2 实现计划**、**架构.md 粘贴用全文**、**扩展 QA/E2E** | **否** — 该部分在对话中已输出，仓库内对应载体主要为 **`docs/多租户隔离-实施交付物-PR清单-PRD架构K8s与QA.md`**（PR 清单、PRD、K8s 示例、Checklist）；更细的差距分析见 **`docs/当前对齐差距报告.md`**。 |

**说明**：此前应你要求仅将「第一步·差距验证」单独落盘，故初版文件较短；本版已扩展为**完整验证报告**（仍不包含 PRD 全文，避免与实施交付物文档重复维护）。

---

## 1. 已执行的核查命令与结果（可重复）

| 核查项 | 命令/范围 | 本次结果 |
|--------|-----------|----------|
| `apps/runner` 是否存在 | Glob `apps/**/runner/**` | **初版：0 个文件**；**合并后：存在 `apps/runner` 包** |
| TS 中 `child_process` / `node:child_process` | Grep 工作区根目录，`*.ts` | **0 匹配** |
| `CommandPolicyEngine` | Grep `*.{ts,tsx,js}` | **初版：0 匹配**；**合并后：`apps/runner/src/policy/command-policy.engine.ts`** |
| `架构.md` 沙箱相关关键词 | Grep `Sandbox` \| `gVisor` \| `Firecracker` | **0 匹配** |
| `架构.md` 租户执行叙述 | Grep `租户执行` \| `Sandbox Space` \| `Runner 为唯一` | **0 匹配** |
| `ENABLE ROW LEVEL SECURITY` | Grep `infrastructure/postgres/migrations/*.ts` | **多文件命中**（与历史一致，未退化） |
| `app.current_tenant` 出现在迁移中 | Grep `infrastructure/postgres/migrations/*.ts` | **至少 20+ 个迁移文件**含该 GUC（策略与租户绑定仍存在） |
| Worker 中 `sandboxId` / `foundry.sandbox` | Grep `apps/worker`，`*.ts` | **0 匹配** |
| `foundry.company_id` span | Grep `autonomous-orchestrator.service.ts` | **第 243、1564 行仍存在** |
| Worker 挂载 `TenantModule` | Grep `apps/worker/src/app.module.ts` | **存在**：`import` + `imports: […, TenantModule, …]` |
| Worker 内 `billing.checkAllowance` / `approval.create` | Grep `apps/worker`，`*.ts` | **多处命中**（如 `pending-agent-tasks.service.ts`、`autonomous-orchestrator.service.ts`、`collaboration-llm-bridge.service.ts`、`agent-execution.service.ts` 等）— 属 **LLM/预算/协作门控**，**非** Shell 统一执行链 |
| 网关 `AuditInterceptor` | Grep `apps/gateway/src/main.ts` | **仍存在**（import + `app.get(AuditInterceptor)`） |
| MinIO 适配器是否强制租户前缀 | Read `minio.adapter.ts` + `StorageService` | **P1**：键由 **StorageService** 解析为 `companies/{companyId}/...` 后再传入适配器；适配器仍做 key 断言，**不在此层拼接 company** |
| Webhooks 是否挂载 `TenantModule` | Read `apps/webhooks/src/app.module.ts` | **imports 中无 `TenantModule`** |
| 红队专门文档 | Glob `docs/**/tenant-red-team*` | **0 文件**（`docs/security/tenant-red-team-plan.md` 未创建） |
| CI 中 RLS 专项门禁 | Grep `.github/**/*.yml`，`rls` / `ROW LEVEL` / `tenant.*coverage`（-i） | **0 命中** |
| 红队相关关键词在 md 中 | Grep `红队` / `red team` / `prompt injection`，`*.md` | 主要命中清单与自有实施文档，**无独立红队计划文件** |

---

## 2. G1～G8 当前状态（含验收口径）

| ID | 项 | 状态 | 说明 | 判定为「已修复」的最低标准（供后续验证） |
|----|----|------|------|------------------------------------------|
| **G1** | `apps/runner` 与唯一命令入口 | **已部分修复** | 已存在 `apps/runner` 与 `runner.execute` 等 RPC；**Worker 业务尚未全部改为唯一入口**（P8） | 可部署 Runner + Worker 侧执行点全部走 `runner.execute`（或等价封装），并具备集成验收 |
| **G2** | CommandPolicyEngine（默认 deny） | **已部分修复** | `apps/runner` 内已实现 | 所有需管控的 **shell 命令**均经 Runner 策略（含 decision id）；与 G1 同步关闭 |
| **G3** | MinIO 对象 key 全链路强制租户前缀 | **已部分修复** | `StorageService` + path util + 单测；适配器纵深断言 | 无绕过路径的直写；对象级 CI/审计可选 |
| **G4** | `架构.md`：Sandbox + 执行面 snapshot/restore | **仍存在** | 无约定关键词/小节 | `架构.md` 含租户执行模型，并区分 DB 配置快照 vs 卷快照 |
| **G5** | Trace：`runId` + `sandboxId` 贯穿沙箱执行 | **仍存在** | 无沙箱；无 `sandboxId` span | Runner 执行链路上 OTel 同时具备 `runId` 与 `sandboxId` |
| **G6** | 红队测试计划与用例落库 | **仍存在** | 无 `tenant-red-team-plan.md` | 独立安全文档 + 可运行用例或 CI 门禁 |
| **G7** | Webhooks 与 TenantModule / 租户上下文 | **仍存在** | `webhooks` 未挂 `TenantModule` | 要么接入租户 CLS + 数据访问规范，要么书面定界「仅平台级」 |
| **G8** | 新增表 RLS 自动化门禁 | **仍存在** | CI 无专项校验 | 脚本或 CI 对须隔离表校验 `rowsecurity`/策略存在 |

**小结**：**G1 / G2 / G3** 自 P7+P1 合并后为 **「已部分修复」**（见上表「说明」列）；**G4～G8** 仍为 **「仍存在」** 或未关闭。完全关闭 G1～G3 需 **P8**（Worker → Runner）及持续防绕过治理。

---

## 3. 与《当前实现对齐检查清单》A～D 的对照（验证结论）

以下将根目录 `当前实现对齐检查清单.md` 中的大类，映射到本次核查结论（细节仍以 `docs/当前对齐差距报告.md` 为准）。

### A. 数据层隔离（TenantModule + RLS）

| 清单关注点 | 验证结论 | 备注 |
|------------|----------|------|
| 核心表 `company_id` 等 | **基线仍成立** | 迁移层大量存在；`users` 无 `company_id` 为**设计例外**（见 `当前对齐差距报告.md` §2.1） |
| RLS + FORCE | **基线仍成立** | `ENABLE ROW LEVEL SECURITY` 多迁移命中；**G8** 仍为自动化缺口 |
| TenantContext / `app.current_tenant` | **基线仍成立** | TypeORM bootstrapper 仍存在；Worker 有 `TenantModule` |
| 全表 Policy / 无绕过 | **未全量复跑代码审计** | 与原文档一致：建议专项 review + G8 |
| Redis 前缀 | **设计取舍** | 业务键 `company:` 文档见 `TenantModule验收说明.md` |
| MinIO 前缀 | **已部分修复（G3）** | `StorageService` 强制写入前缀；防直写绕过仍属治理项 |
| 硬编码绕过 | **未全量复跑** | Webhooks **G7** 仍为显式疑点 |

### B. 执行层隔离（Worker / API）

| 清单关注点 | 验证结论 | 备注 |
|------------|----------|------|
| Worker 内 `child_process` / Docker exec | **本次仍为 0 匹配** | 不降低 **G1**（尚无 Runner「唯一入口」） |
| `apps/runner` | **已部分修复** | **G1** 待 P8 闭环「唯一入口」 |
| CommandPolicyEngine | **已部分修复（在 Runner）** | **G2** 待与 G1 同步覆盖全部命令路径 |
| Approval + Billing 于执行前 | **部分能力存在** | 预算/LLM/任务门控有 RPC；**不等价**于 Shell 执行链 |

### C. 架构文档一致性

| 清单关注点 | 验证结论 | 备注 |
|------------|----------|------|
| 每公司 Sandbox Instance | **未写入架构** | **G4** |
| 各域绑定公司空间（执行面） | **模块有、执行空间未建模** | 与原文档一致 |
| workspace snapshot / restore | **执行面未描述** | DB `company_snapshots` 不等于卷快照 |

### D. 安全与可观测性

| 清单关注点 | 验证结论 | 备注 |
|------------|----------|------|
| OTel 关联 sandbox | **仍无法成立** | **G5** |
| 红队计划 | **仍无专门交付物** | **G6** |
| 高危操作审计 | **网关审计仍在** | 沙箱命令审计规范仍缺 |

---

## 4. 新增差距与回归说明

- **新增差距**：本次复跑 **未发现** 相对 `docs/当前对齐差距报告.md` 的**新类别**问题（无「上次没有、这次出现」的条目）。  
- **回归**：**未发现** 已修复项被回退（因当前无 G1～G8 关闭项，故无回归可列）。  
- **强化证据**：相对初版落盘文件，本次补充验证了 **`TenantModule` 仍在 Worker**、**`billing.checkAllowance` / `approval.create` 仍在 Worker 多文件**、**迁移中 `app.current_tenant` 仍广泛存在**、**网关 `AuditInterceptor` 仍在**，避免「只有负面扫描」的片面感。

---

## 5. 未在本次自动复跑的项目（沿用基线）

以下依赖人工或集成环境，**未**作为本次脚本级核查的一部分，结论沿用 `docs/当前对齐差距报告.md`：

- 全库 `createQueryBuilder` 是否遗漏 `company_id`。  
- Worker **每一个** MQ 消费者是否均 `runWithCompanyId`。  
- 生产库中 RLS 是否被超级用户绕过等运行时策略。

---

## 6. 文档索引（避免内容只存在于聊天）

| 需求 | 建议阅读的仓库文件 |
|------|-------------------|
| 完整静态差距分析（表格式 A～D + 参考路径） | `docs/当前对齐差距报告.md` |
| P1～P8、阶段 1～2 PRD、K8s 示例、QA Checklist | `docs/多租户隔离-实施交付物-PR清单-PRD架构K8s与QA.md` |
| Tenant 策略与测试指引 | `docs/TenantModule验收说明.md` |
| 原始检查清单 | `当前实现对齐检查清单.md` |

若需将对话中的 **P1～P8 详细方案 / 架构小节全文 / 扩展 E2E** 再单独落为一个 `docs/` 文件，可指定文件名，避免与 `多租户隔离-实施交付物…` 重复时可改为「架构-租户执行模型.md」「多租户-PR实施方案.md」等拆分。

---

## 7. 建议下一步

1. **P8**：按 [`docs/p8-worker-runner-migration-plan.md`](p8-worker-runner-migration-plan.md) 将 Worker 执行点迁移至 `runner.execute`，关闭 G1/G2 剩余口径。  
2. 按 `docs/多租户隔离-实施交付物-PR清单-PRD架构K8s与QA.md` 中其余项排期。  
3. 合并涉及 **新表、文件存储、Webhooks** 等的 PR 后，更新本文件 **§1 日期**并复跑核查表。  
4. 可选：将 §1 固化为 `pnpm`/CI 脚本，保证验证可重复。

---

*本文件由 2026-04-12 仓库静态核查生成；扩展版合并了初版「仅第一步摘要」与完整验证对照。*
