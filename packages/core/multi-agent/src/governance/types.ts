export type GovernanceFeatureFlag = 'ENABLE_ADVANCED_APPROVAL';

export type ApprovalFlowStatus = 'running' | 'approved' | 'rejected' | 'expired' | 'cancelled';

export type BoardDecisionStatus = 'open' | 'passed' | 'rejected' | 'cancelled' | 'expired';

